WHY
This rework was triggered because TASK-292 Gap 2 GSD did not update the
queue-pool in parallel. Gap 3 was the cross-process flock issue from the
prior session. We also noticed Gap 4 appear in the context replay.

WHAT
Fix the spawn worker bootstrap to update both the queue path and the pool.

ACCEPTANCE
- spawn worker updates queue-pool in parallel with queue path
- regression test covers the BUG-022 repro
