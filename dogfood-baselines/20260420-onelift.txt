=== onelift ===

--- gg doctor ---
  ✓ AGENTS.md                    /Users/gurkangul/my-projects/onelift/AGENTS.md
  ✓ agents_schema                up to date (2.1)
  ✓ index-state.json             present
  ~ .gg/.gitignore               stale — missing entries: runtime/, *.seq, embedding-meta.json (run `gg doctor --heal` to fix)

Outbox (index pipeline crash-safety):
  ✓ outbox                       empty — stores consistent

──────────────────────────────────────────────────
All checks passed.
  ⚠ 2 artifact(s) drifted from CLI templates — run `gg doctor --sync-artifacts` to inspect

Paste this into your AI agent's chat (works for any agent):

────────────────────────────────────────────────────────────
I am operating inside a gg-cli enforced project.
Before anything else:
  1. export GG_AGENT=claude-code   # use your agent's name
  2. Run: gg status
  3. Read AGENTS.md at the repo root.
  4. From now on use gg for every decision, task, rejection,
     and cross-agent handoff. No exceptions.
────────────────────────────────────────────────────────────

Install agent hooks: `gg doctor --install-agent-hooks`

--- gg audit inbox-obedience --since 7d ---
Inbox Obedience  (window: 7d)
────────────────────────────────────────────────────────────
  Role              Received  Acknowledged    Ratio  Status
────────────────────────────────────────────────────────────
  all                    608           524      86%  OK
  developer               22            22     100%  OK

--- gg audit file-size ---
file-size: 2 violation(s)

  FILE                                                           LINES   LIMIT
  ------------------------------------------------------------  ------   -----
  lift-cli/test/e2e/regression_test.go                            1182     800
  lift-cli/internal/manifest/schemagen/types_gen.go                596     500

Split oversized files into cohesive modules. Re-run to verify.

--- gg doctor --bypass-audit --bypass-since 7d ---
Bypass Audit
──────────────────────────────────────────────────────────────────────
  window: since 7d (cutoff 2026-04-13T18:27:55Z)
  source: /Users/gurkangul/.gg/projects/f0431c81-6ca3-4fe6-8478-5275fe40f5db/state.json
──────────────────────────────────────────────────────────────────────
No bypass events recorded in this window. ✓

--- gg status (tail — telemetry) ---
  ○ TASK-237 [high] TASK-237: TASK-236 reopen — secret sync path'i canlıda hala aktif değil
  ○ TASK-238 [high] TASK-238: Auto-deploy secret sync — LIVE DEBUG zorunlu, iki teşhis turu yanlış çıktı
  ○ TASK-239 [medium] TASK-239: AutoDeploy 202 OK ama container com.onelift.sha label 5dk içinde update olmuyor

BUGS:
  ● Open: 0  ⚙ Fixing: 0  ↻ Reopened: 0  ✓ Fixed: 0  ⊘ Wontfix: 3

MESSAGES:
  Unread: 1
  [claude-code → all] TASK-239 picked up — auto-deploy SHA label timeout debug
  (run `gg inbox` to mark as read)

RECENT DECISIONS:
  • TASK-238 root cause: git_connect.go secret write failure is non-fatal warning — runWebhookInstall proceeds even if secret file not written. webhook serve then finds no secret (manifest omitempty + file missing) and exits 0. Fix: (1) make secret write failure fatal in server-side block, (2) runWebhookInstall must validate secret exists before installing daemon
    Tags: TASK-238, bug, webhook, hmac, secret-sync, fatal-error
  • TASK-235 root cause: remoteLogsSSH builds 'docker logs $(docker compose ps -q api)' — if subshell returns empty string, docker logs hangs waiting for stdin. Fix: wrap in shell guard to validate container ID before calling docker logs
    Tags: TASK-235, bug, logs, docker-compose, multi-service
  • TASK-237 root cause: git_connect.go MkdirAll error silently swallowed + webhook daemon reads secret once at startup into closure
    Tags: TASK-237, bug, webhook, hmac, secret-sync
  • TASK-236: runWebhookInstall now uses runUserSystemctl helper that injects XDG_RUNTIME_DIR and DBUS_SESSION_BUS_ADDRESS for SSH non-interactive sessions. restart failure is now fatal (return fmt.Errorf) instead of just Fprintf.
    Tags: TASK-236, bugfix, webhook, hmac, systemd, ssh-env
  • TASK-235: purgeServer now waits for docker-proxy to exit (up to 5s, 500ms intervals) before returning. Root cause: docker-proxy holds host-port binding after container removal; next test's docker run races against it and gets EADDRINUSE even though docker ps -aq returns 0. Fix: server.go poll loop after container cleanup.
    Tags: TASK-235, bugfix, test-isolation, docker-proxy, port-binding

RECENT REJECTIONS:
  ✗ Test matrix'ine otomatik nip.io (veya benzeri wildcard DNS) test-domain ekleme
  ✗ head -N / tail -N / truncate approach on shared list configs (authorized_keys, hosts, systemd unit lists, etc.)
  ✗ Wave 1 reject: do NOT copy cloud-project manifest.Write pattern of 4 sequential exec.Run calls (tmp write, chmod, chown, mv). For local filesystem use os.WriteFile+os.Rename in one function. For SSH wave later, batch via single sh -c to avoid round-trip latency. 4 round-trips per deploy × 50 deploys = a lot of SSH overhead.

TELEMETRY (last 7 days — 1868 calls, 29% agent-initiated):
  verify           252
  track            222
  inbox            211
  tell             186
  report           142
  get              129
  create           123
  done             110
  session-start    108
  record           85
  list             61
  run-repros       61
  status           53
  search           41
  ready-for-live   26
  decisions        17
  doctor           17
  block            8
  reindex          4
  wontfix          3
  impact           2
  backfill         1
  decide           1
  deps             1
  file-size        1
  inbox-obedience  1
  note             1
  reindex-decisions 1

File-size drift: 2 new violation(s) (baseline frozen at 0) — run `gg audit file-size`
