## gg graph export

Export a self-contained offline graph visualization

```
gg graph export [flags]
```

### Options

```
      --format string    export format: html (default "html")
  -h, --help             help for export
      --symbol-cap int   include symbol view only when symbol count is at or below this cap (default 500)
```

### Options inherited from parent commands

```
      --json   output results as JSON
```

### SEE ALSO

* [gg graph](gg_graph.md)	 - Work with the local code graph

